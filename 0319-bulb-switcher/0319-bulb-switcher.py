import math
class Solution:
    def bulbSwitch(self, n: int) -> int:
        
        ## depends on the number of divisors, all numbers have even number of divisors
        ## except the square number
        
        
        return int(math.sqrt(n))
        